{
  "name": "datasport-results-analyzer",
  "version": "1.0.0",
  "description": "Generate SVG visualizations (netto times, histogram, start buckets) from results.json",
  "type": "module",
  "private": true,
  "license": "MIT",
  "scripts": {
    "netto-times": "node netto-times.js",
    "histogram-netto-times": "node histogram-netto-times.js",
    "start-buckets-stacked": "node start-buckets-stacked.js",
    "graphs": "node netto-times.js && node histogram-netto-times.js && node start-buckets-stacked.js",
    "clean": "rm -f *.svg"
  },
  "devDependencies": {}
}
